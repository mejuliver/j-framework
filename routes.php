<?php

	if(!defined('pixzel')) :
		die('You have no candy, get off my lawn!');
	endif;

	$url = false;

	// Defines for app use
	define( 'url', $core->url );
	define( 'theme_dir', $core->url.'/' );
	define( 'components_dir', $core->url.'/public/');
	define( 'plugins_dir', $core->url.'/public/plugins/');
	define( 'css_dir', $core->url.'/public/css/');
	define( 'js_dir', $core->url.'/public/js/');
	define( 'media_dir', $core->url.'/public/media/');

	// -------------------------------------------------------------
	//defined routes here



	// -------------------------------------------------------------

